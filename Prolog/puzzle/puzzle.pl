solve(F1, W1, G1, C1, F2, W2, G2, C2) :-
    %% remove fail and add body/other cases for this predicate
    fail.


state(F,W,G,C) :-

opposite(A,B).
opposite(B,A).

%if state A is unsafe.
unSafe(A) :-

safe(A) :-

take(X, A, B) :-

arc(N, X, Y) :-
